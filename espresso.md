# Espresso

**onView**(**ViewMatcher**)
    .**perform**(**ViewAction**)
  
**onView**(**ViewMatcher**)
    .**check**(**ViewAssertion**)
    
**onData**(**ViewMatcher**)
    .**DataOptions**
    .**perform**(**ViewAction**)
    .**check**(**ViewAssertion**)
    
## View Matchers

## View Assertions

## Data Options
```kotlin
inAdapterView(Matcher)
atPosition(Integer)
onChildView(Matcher)
```
